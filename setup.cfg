[metadata]
name = tree_comparison
version = attr: tree_comparison.__version__

[options]
packages = find:
python_requires = >=3.9

[options.entry_points]
console_scripts =
    tree-compare = tree_comparison.tree_compare:console_script
