[metadata]
name = arbors
version = attr: arbors.__version__

[options]
package_dir =
    = src
packages = find:
python_requires = >=3.9

[options.packages.find]
where = src

[options.entry_points]
console_scripts =
    arbors = arbors.tree_compare:console_script